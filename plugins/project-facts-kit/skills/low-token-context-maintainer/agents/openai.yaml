interface:
  display_name: "Low Token Context"
  short_description: "Route ambiguous workspaces and keep local fixes lean"
  default_prompt: "Use $low-token-context-maintainer to choose the lightest safe context path for this workspace task."

policy:
  allow_implicit_invocation: true
